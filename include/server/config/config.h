#include "hard.h"

extern char server_desc[BEDROCK_MAX_STRING_LENGTH];
extern int server_maxusers;
extern char server_ip[64];
extern int server_port;
extern bool allow_new_users;

extern int config_parse(const char *config);

